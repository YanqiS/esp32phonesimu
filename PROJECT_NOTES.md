# ESP32 车机“手机模拟器”可行性梳理

## 现在代码能做什么
- `mcu_bt_phone.c` 走的是 **Classic Bluetooth HFP AG（Audio Gateway）** 角色，也就是把 ESP32 伪装成“手机端”。
- 车机如果作为 **HFP HF（Hands-Free）**，理论上可以与 ESP32 建立配对、连接、接听、挂断、发起拨号。
- 代码里已经包含：
  - 模拟来电。
  - 车机发起拨号后的外拨流程。
  - 接听、拒接、挂断。
  - SCO 音频连接建立/断开。

## 这次做的调整
- 将 `mcu_bt_phone.c` 的 LED 与旋钮引脚配置对齐到 `mcu1_led.c`。
- 新增旋钮状态机：
  - 蓝牙改为上电自动启动，避免因为未按 `BOOT_KEY` 导致手机/车机扫描不到设备。
  - 右旋钮 `1 -> 0`：模拟来电（默认号码 `13800138000`）
  - 右旋钮 `2 -> 0`：接通当前振铃/来电
  - 右旋钮 `3 -> 0`：挂断/拒接/取消外拨

## 关键现实限制
1. **能否被车机识别，取决于车机是否接受标准 HFP AG。**
   如果车机做得很“严”，可能还会额外检查：
   - 设备类别（CoD）
   - 设备名/厂商特征
   - SDP 记录是否像真实手机
   - 是否同时支持 PBAP / MAP / A2DP / AVRCP
   - 某些车机会先尝试 A2DP / AVRCP，若只看到 HFP 可能直接断开
   - 某些 AT 命令响应是否完整

2. **手机能配对但通常不会“连上电话功能”，这在 HFP AG 方案里是正常现象。**
   普通手机本身也是 AG（手机端），而不是 HF（车机/耳机端），所以它往往只能完成经典蓝牙配对，不能像车机那样建立 HFP 电话连接。

3. **“随便拨 11 位号码能打通”只能做到“流程打通”，不是接入真实蜂窝网络。**
   目前实现的是：
   - 向车机报告“正在外拨”
   - 延时后改成“已接通”
   - 建立 SCO 音频
   这适合测试车机 UI、状态同步、蓝牙接打电话流程；
   但不代表真的打到公网号码。

4. **如果车机要求更像真手机，后续可能要补协议。**
   - 仅靠 HFP 里的号码/通话状态同步，很多车机只能知道“正在通话/拨号”，但未必会显示完整通讯录；若要稳定显示联系人列表，通常还需要 PBAP。
   - 部分车机只有在 SCO 音频链路也建立成功后，才会弹出完整通话界面；如果只建立了 SLC 但音频始终未连上，车机可能只亮电话图标。
   - 若日志持续出现 `Remote does not support 3-EDR eSCO`，可优先尝试关闭 HFP 宽带语音（mSBC / WBS），强制走更保守的 CVSD 窄带语音。
   优先级建议：
   - 先验证 HFP AG 是否能稳定连接。
   - 再补全更完整的指示器/AT 响应。
   - 如车机还需要联系人/通话记录，再考虑 PBAP / MAP。

## 建议测试路径
1. 先烧录 `mcu_bt_phone.c`，确认车机能搜到设备并完成配对。
2. 验证以下动作：
   - 车机主动连接/断开蓝牙
   - 按键模拟来电
   - 车机接听/挂断
   - 车机端发起拨号
   - 旋钮 `1 -> 0 / 2 -> 0 / 3 -> 0`
3. 如果车机搜得到但不认作手机：
   - 抓车机侧蓝牙日志
   - 补设备类别/SDP/AT 指令兼容
   - 必要时同时打开 A2DP/AVRCP 伪装成“更像手机”的复合设备

## 结论
**能做到“用于车机蓝牙电话流程测试的 ESP32 手机模拟器原型”，而且当前方向是对的。**

但如果你的目标是：
- 适配“非常挑剔”的未知车机；
- 被对方稳定识别成“真手机”；
- 覆盖所有电话与媒体联动场景；

那么大概率还需要继续补蓝牙画像与协议细节，而不只是 HFP AG 单点可用。

## menuconfig 调整建议
- 可以，**这个问题非常适合先从 `menuconfig` 下手**。当前仓库已经通过 `sdkconfig.defaults` 默认关闭了 HFP 宽带语音并把音频数据路径设为 HCI。
- 你本地可以优先检查或调整这两项：
  - `Component config -> Bluetooth -> Bluedroid Options -> HFP wide band speech`：建议先关闭，避免走 mSBC / eSCO。
  - `Component config -> Bluetooth -> Bluedroid Options -> HFP audio data path`：建议使用 `HCI`。
- 如果你修改过 `sdkconfig`，请在重新构建前执行 `idf.py fullclean`，否则旧配置可能继续生效。
- 如果后续确认车机其实支持更高规格音频，再考虑重新打开 WBS 做对比。


## 双 ESP32 互打电话方案
- **可以尝试，但不能简单理解成“两个都伪装成手机然后彼此直接通话”。**
- 更合理的角色分工通常是：
  - **ESP32-A**：作为“车机眼里的手机”，负责和车机建立 HFP / A2DP / AVRCP 连接。
  - **ESP32-B**：作为“远端网络 / 另一部手机的状态模拟器”，不一定直接通过蓝牙和车机通信，而是通过串口 / Wi‑Fi / ESP-NOW / BLE / GPIO 与 ESP32-A 同步通话状态。
- 原因是：经典蓝牙 HFP 里，真正能和车机直接配对并提供电话服务的一侧，本质上还是一个 **HFP AG（手机端）**。如果两块板子都只做 AG，它们之间并不会天然形成“像真实电话网络那样”的呼叫链路。
- 因此更推荐的方案是：
  1. 车机只连 **ESP32-A**。
  2. ESP32-A 对车机暴露“来电 / 外拨 / 通话中 / 挂断”等标准 HFP 状态。
  3. ESP32-B 只负责触发“远端来电、远端接听、远端挂断、远端占线”等事件。
  4. A/B 两块板子之间用你最方便的板间通信方式同步事件。
- 这样就能实现你要的测试闭环：
  - 车机拨号给“远端” → ESP32-A 把请求转给 ESP32-B → ESP32-B 回报“振铃 / 接听 / 挂断”。
  - 远端发起呼叫 → ESP32-B 通知 ESP32-A → ESP32-A 向车机报告来电 → 你在车机上接听 / 挂断。
- **如果目标是稳定做车机测试，这个双板架构比“两个都纯蓝牙伪装成手机互打”更可控，也更容易调试。**


## 双板“互相打电话”是怎么实现的
- 这里的“互相打电话”本质上不是两块 ESP32 真的像两部蜂窝手机那样互连运营商网络，而是 **两块板子协同模拟一次电话流程**。
- 关键点是把“蓝牙手机接口”和“远端呼叫状态”拆开：
  - **ESP32-A（车机侧）**：只负责把 HFP / A2DP / AVRCP 状态呈现给车机。
  - **ESP32-B（远端侧）**：只负责扮演“对端用户 / 对端网络”，告诉 A 板“现在在振铃、现在接听了、现在挂断了”。
- 当 **车机发起拨号** 时，流程可以是：
  1. 车机通过 HFP 把号码发给 ESP32-A。
  2. ESP32-A 把这个“拨号请求”通过板间通信发给 ESP32-B。
  3. ESP32-B 决定当前远端状态，例如：振铃 / 接听 / 拒接 / 占线。
  4. ESP32-B 把结果回传给 ESP32-A。
  5. ESP32-A 再把这些状态翻译成 HFP 的 `CIEV` / `CLCC` / 音频连接事件反馈给车机。
- 当 **远端发起来电** 时，流程反过来：
  1. ESP32-B 先产生“来电事件”。
  2. ESP32-B 把来电号码和状态发给 ESP32-A。
  3. ESP32-A 向车机报告来电（RING、号码、来电状态）。
  4. 你在车机上按接听 / 挂断后，ESP32-A 再把这个结果同步给 ESP32-B。
- 也就是说，**真正和车机打交道的永远是 ESP32-A**；ESP32-B 不一定要和车机蓝牙连接，它更像“电话网络和远端用户的状态脚本机”。
- 这套架构的本质是：
  - A 板负责“让车机看到一部手机”。
  - B 板负责“让这部手机背后真的有个远端在变化”。
- 所以“互相打电话”其实是 **A/B 两块板子通过板间协议协同，向车机演出一场完整的呼叫过程**，而不是它们自己像真实手机那样直接拨打公网电话。


## “A 转给 B”到底是怎么转的
- 这里的“转给 B”**不是像真实手机那样通过运营商网络去拨打一个真正的电话号码**。
- 例如你可以在逻辑上约定：
  - `12345678999` 代表 A 板侧的测试号码
  - `12345678912` 代表 B 板侧的测试号码
  但这些号码在系统里更像 **业务标识符**，不是公网可达号码。
- 真正发生的事情是：
  1. 车机把“拨打 12345678912”这个动作通过 HFP 发给 **A 板**。
  2. A 板在本地解析出“目标是 B 板侧脚本”。
  3. A 板通过板间通信把一条消息发给 B 板，例如：`{event: dial, number: 12345678912}`。
  4. B 板根据自己的脚本决定下一步：振铃 / 接听 / 拒接 / 占线。
  5. B 板再把结果回给 A 板，例如：`{event: alerting}` 或 `{event: answered}`。
  6. A 板把这个结果翻译成车机能看懂的 HFP 呼叫状态。
- 所以，**号码本身只是 A/B 之间约定的“场景触发器”**。真正的“呼叫转发”靠的是两块板子之间的本地通信，不是公网电话网络。
- 板间通信可以是：
  - UART 串口（最容易先做通）
  - Wi‑Fi / UDP
  - ESP-NOW
  - BLE
  - 甚至简单 GPIO 脉冲（不推荐做复杂状态）
- 如果你想把它做得更像“真的两部手机”，可以在协议里约定：
  - A 板有自己的逻辑号码表
  - B 板有自己的逻辑号码表
  - A 板拨到某个号码时，映射到 B 板的某个联系人或脚本
  但底层依然是 **板间消息传递 + HFP 状态翻译**，不是蜂窝网络通话。
